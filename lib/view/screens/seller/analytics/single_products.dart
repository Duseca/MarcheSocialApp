import 'package:flutter/material.dart';

class SingleProducts extends StatelessWidget {
  const SingleProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
